import type { FunctionComponent } from "../common/types";

export const NotFoundPage = (): FunctionComponent => {
    return(
        <div>
            <h1>decreasme the there</h1>
        </div>
    );
};